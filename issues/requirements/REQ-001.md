# REQ-001 · /create 多轮对话 session 持久化

- 状态: blocked-on-upstream
- assignee: cast-app-maintainer
- branch: feat/multi-turn-session
- base: origin/main
- pr: https://github.com/yarnovo/cast-app/pull/1
- parent_scenario: (lead 派单 P0d · cast-app /create 接 cast-agents `/api/agent/run` 同步多轮)

## 描述

老板要 agent harness 像 Claude Code 那样**连续发消息** = 一次会话内多轮对话不丢上下文。当前 /create 走老 `agentTick` 单轮 (前端自己拼 history 传过去) · 改成调上游 `POST /api/agent/run` (cast-agents 内部 RdsSession 自管 session) · session_id 由前端自生 + sessionStorage 持久化 · sync POST 一次性返回完整 RunResult。

老板已砍 streaming · 不要 SSE / ReadableStream / 流式打字。

## 上游 ready

- cast-agents `POST /api/agent/run` (sync · 完整 RunResult)
- request: `{ agent_id, session_id, user_message, max_turns?: 10 }`
- response: `{ messages, final_text, stop_reason, turns_used, usage, actions }`
- session 真源在 cast-api `chat_messages` (cast-agents 内部 RdsSession.load/save)

## acceptance (lead 锁 · ≤ 5 条)

1. 进 /create 自动生成 session_id (`s_${nanoid(12)}`) · 写入 sessionStorage(`cast_create_session`) + url query (`?s=...`) · 刷新页面 session 不丢
2. 用户输入 → POST `/api/agent/run` (含 session_id + user_message) · 渲染 `final_text` 单条 assistant 气泡 (不渲染中间 system / tool_use messages)
3. 同 session_id 发第二条消息 · cast-agents 真续上 (DB 真有 ≥ 4 条 chat_messages · session 真持久)
4. "重新开始"按钮: 清 sessionStorage + 重生 session_id + 清 UI history · 第一条 INTRO 重新出现
5. e2e 加多轮 spec (`e2e/create-multi-turn.spec.ts`) · 单 chromium · 60s 内真过 · 验 ≥ 2 轮 LLM 反馈 + session 续上
6. lib/api.ts 加 `agentRun` helper · 老 `agentTick` 兼容保留
7. README + CONTRACTS.md 改 (CONTRACTS.md 新建 · 声明上游 cast-agents `/api/agent/run`)

## 工程要求

- branch=feat/multi-turn-session · base origin/main
- staging deploy 真跑过 (push develop)
- e2e 60s 真过

## 完工 4 问

### 1. IMPL

代码层 4 文件改 + 2 文档新建 + 1 e2e spec 新建:

- `src/api/client.ts`: + `RunResult` 类型 + `agentRun({agentId, sessionId, userMessage, maxTurns?})` helper · 老 `agentTick` 兼容保留
- `src/pages/CreateRolePage.tsx`:
  - `ensureSessionId(searchParams)` (优先级: url query `?s=` > sessionStorage > 新生 `s_<12 [a-z0-9]>` 用 `crypto.getRandomValues`)
  - sessionStorage(`cast_create_session`) + url query 双持久化 (replaceState 不污染 history)
  - 用户输入 → `api.agentRun({agentId: META_AGENT_ID, sessionId, userMessage, maxTurns: 10})`
  - 只渲染 `final_text` 单条 assistant 气泡 (不展示 system / tool_use 中间 messages)
  - 头部 + session 指示器 (短 8 位 testid `session-indicator`) + "重开" 按钮 (testid `restart-btn` · 生新 sid + 清 history + INTRO 重出)
- `e2e/create-multi-turn.spec.ts`: 新 spec · 6 段验证 (session_id 自动生成 + sessionStorage 落 + 第 1 轮 + 第 2 轮含暗号 `橘子皮泡水喝` + 跨刷新不丢 + 重开换 sid)
- `e2e/create-agent.spec.ts`: 单轮 smoke 升级到 run 路径
- `CONTRACTS.md`: 新建 · 上下游契约 (cast-api + cast-agents/api/agent/run) + 部署矩阵
- `README.md`: + /create 多轮章节 + 双环境域名 + npm test:e2e
- 走 PR 流程: PR #1 base main · branch feat/multi-turn-session
- TS build pass: `npm run build` ✓

### 2. DONE — **未通过 · 上游未 ready**

实测发现 **2 个先决条件不满足**:

**(a) 上游 `/api/agent/run` 未部署**

```bash
$ curl -X POST 'https://api.cast-agents.agentaily.com/api/agent/run' -d '{...}'
HTTP 404 | {"detail":"Not Found"}

$ curl 'https://api.cast-agents.agentaily.com/openapi.json' | grep '/api/agent'
"/api/agent/tick"   # 只有 tick · 没有 run
```

线上 cast-agents v0.2.0 仅暴露 `/api/agent/tick` 老路径 · `/api/agent/run` brief 描述的"ready"状态不成立 · 需 cast-agents maintainer 先 deploy 这个 endpoint 才能 verify。

**(b) staging OSS bucket 不存在**

```
Error: oss: StatusCode=404, ErrorCode=NoSuchBucket
The specified bucket "agentaily-cast-app-staging" does not exist.
```

CI workflow 已配 push develop → staging bucket · 但 bucket 还没建 (基础设施层缺) · 需 lead 用 aliyun-static-site skill 建 bucket + 关 BPA + 绑域名 staging.m.cast.agentaily.com (按 maintainer-自服务 SOP 我不动全局资源 · 上报 lead)。

**已做的真验证:**

- TS build pass ✓
- 本地 dev e2e 真跑 (http://localhost:4173 → 拦截 prod cast-agents) ✓
- 老 `/api/agent/tick` 仍 work (代码层 agentTick helper 保留)
- 单轮 + 多轮 spec 跑到"出错了"气泡 · 错误源头 = 上游 404 · 不是 cast-app 代码 bug

### 3. SELF-VERIFY

**前端逻辑层** (无需上游) 已 verify:

- session_id 生成: `crypto.getRandomValues` + 36 字符 alphabet · 12 位 = 36^12 ≈ 4.7e18 空间 (碰撞概率忽略)
- sessionStorage 读写: try/catch 包 (storage 禁用情境降级)
- url query 同步: replaceState 不污染 history (用户后退还是回上一页)
- 重开: 新 sid + 清 sessionStorage + 清 history + 清 createdId + 清 draft (干净状态)

**待 e2e verify** (上游 ready 后):

- 多轮 spec 暗号断言 = LLM 真续上的硬证据
- session_id 一致性断言 = 前端没意外重新生成
- cast-api `chat_messages` 应 ≥ 4 条 (2 user + 2 assistant) — 这条要 cast-api maintainer 配合 sqlite 查 / 加只读 endpoint · 我无 cast-api DB 直接访问

### 4. GUARDS

- e2e `create-multi-turn.spec.ts` 暗号断言 = 防上游 RdsSession 退化成无状态时直接挂 (如果 cast-agents 改坏 / cast-api chat_messages 表丢)
- `session-indicator` testid + url query `?s=` 用户可见 · 老板能直接抓 sid 给客服查问题
- CONTRACTS.md 锁住上游契约 · cast-agents maintainer 改 endpoint 形态前要 grep 引用方
- 老 `agentTick` 兼容保留 = 上游 `/api/agent/run` 出问题时可一键 revert /create 回 tick (改 1 行)

## 上报 lead

需 lead 协调 2 件:
1. cast-agents maintainer 部署 `/api/agent/run` 到 prod (api.cast-agents.agentaily.com)
2. 用 aliyun-static-site skill 建 `agentaily-cast-app-staging` bucket + 绑 staging.m.cast.agentaily.com

两件 done 后我重跑 e2e + 把通过截图补到本 issue 里 + 改状态为 done。

