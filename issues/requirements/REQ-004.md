# REQ-004 · 默认入口 / 改成跟阿空小造对话页 + 砍 ➕ (温和版)

- 状态: in-progress
- assignee: cast-app-maintainer
- branch: feat/default-meta-chat
- base: origin/main (含 PR #3 revert · 916a712)
- parent_scenario: (lead 5-9 派单 v3 · 老板原话: "之前的页面呢 我只是要你去掉中间的+号，然后我的页面 换成对话对页面啊"; PR #3 砍太狠 · revert 后温和重做)

## 描述

老板 5-9 第 3 次纠正 · 比 REQ-003 (单页对话化) 温和:

- **保留**所有平台多页 (HomePage feed / MarketPage / MessagesPage / MePage / RoleDetailPage / ServiceDetailPage)
- **保留** BottomNav (4 tab)
- **只**改 2 件:
  1. 砍 BottomNav 中间 ➕ 号 (`/create` 路由 + CreateRolePage 一并砍)
  2. 默认入口 `/` 从 HomePage feed → ChatPage (跟阿空小造对话)

→ 用户体验: 打开 `m.cast.agentaily.com` → 直接进对话 (不再看 feed) · 但可通过 BottomNav 切到其他 tab。

老 HomePage 路由搬到 `/home` (保留代码 + 直链可达 · 不进 BottomNav)。后续要不要重新引入 feed tab 老板再拍。

## 上游 ready

- cast-agents `POST /api/agent/run` (sync · 多轮 session 持久化) — 不变
- ag_builtin_meta-xiaozao builtin agent — 不变

## acceptance (lead 锁 · ≤ 7 条)

1. 新增 `src/pages/ChatPage.tsx` (沿用原 CreateRolePage 多轮 session 逻辑 · agentId hardcoded `ag_builtin_meta-xiaozao` · 包在 AppShell 内 · BottomNav 可见)
2. `src/main.tsx` 路由表: `/` → ChatPage · `/home` → HomePage · 其他不变 · 砍 `/create` 路由
3. `src/pages/CreateRolePage.tsx` 砍干净 · `e2e/create-*.spec.ts` 砍干净
4. `src/components/AppShell.tsx` BottomNav: 砍中间 `/create` ➕ 项 · 4 tab (对话(/) / 市集(/market) / 消息(/messages) / 我(/me))
5. `src/pages/MePage.tsx` + `HomePage.tsx` 砍 `/create` 跳转 · 改跳 `/` (找阿空小造)
6. ChatPage 多轮真续上 (sessionStorage `cast_session` + url query `?s=` 双持久化 · 暗号 turn 2 验)
7. e2e: 加 `e2e/default-route-meta-chat.spec.ts` (8 段断言: chat-header / sid 持久化 / INTRO / BottomNav 4 tab 无 ➕ / 2 轮真续 / 重开换 sid)
8. README + CONTRACTS.md 路由表更新 · close PR #2 (REQ-002 stale)

## 工程要求

- branch=feat/default-meta-chat · base origin/main (revert 后状态 916a712)
- TS build pass
- e2e `default-route-meta-chat.spec.ts` 真过 (单 chromium · 60s 内)
- staging deploy 真跑过 (push develop · 看 staging.m.cast.agentaily.com)
- 不动 cast-api / cast-agents

## 完工 4 问

待完工后填。
