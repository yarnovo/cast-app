# REQ-003 · cast-app 单页对话化 (砍所有页 · 留 ChatPage)

- 状态: in-progress
- assignee: cast-app-maintainer
- branch: feat/single-page-chat
- base: origin/main
- parent_scenario: (lead 派单 5-9 · 老板拍 · cast-app 整体砍成 ChatGPT / Claude Code CLI 风单页对话 · 之前 REQ-002 "聊天列表 + 置顶 meta" 那一版同步砍掉 · PR #2 close)

## 描述

老板 5-9 拍**比 REQ-002 更激进**:

- 砍所有 list/feed/profile/detail 页 (HomePage / MarketPage / MePage / MessagesPage / RoleDetailPage / ServiceDetailPage · 还有之前 REQ-002 加的 ConversationDetailPage)
- 砍 BottomNav · 砍 AppShell
- 整个 app = 1 个 ChatPage (路由 `/`) · 默认接 `ag_builtin_meta-xiaozao` 阿空小造对话
- 用户视角: 打开 `m.cast.agentaily.com` → 直接看到跟阿空小造的对话 · 输入"我要造一个健身教练" → meta 引导造
- 后续要"管理" (查/改 agent · 看 post · 下单) 都跟 meta 聊 · meta 调对应 tool · 不再有专门 UI

本 PR **只改 UI · 不动 meta yaml / 不加新 tool**。后续 meta tool 扩展 (list_agents · update_agent · query_posts) 是另一轮。

## 上游 ready

- cast-agents `POST /api/agent/run` (sync · 完整 RunResult) — 不变
- ag_builtin_meta-xiaozao builtin agent — 不变

## acceptance (lead 锁 · ≤ 7 条)

1. `src/pages/` 只留 `ChatPage.tsx` · 其他 7 个页 (HomePage / MarketPage / MePage / MessagesPage / RoleDetailPage / ServiceDetailPage / ConversationDetailPage · 还有之前 P0d CreateRolePage 如果还在的话) 全砍干净 · `src/components/` 砍 AppShell.tsx
2. `src/main.tsx` 路由表只剩 1 条 `/` → ChatPage (+ `*` fallback 也指 ChatPage · 防 OSS SPA 子路由 404 残留 link)
3. `src/api/client.ts` 砍干净业务 endpoint (marketRoles / roleDetail / orders / users) · 只留 `agentRun` helper + 必要类型 (RunResult / RunMessage / RunUsage / TickAction)
4. ChatPage 极简结构: top header (meta 头像 + 名 + 重开按钮 + 用户头像) · main (气泡列表 testid `bubble-{i}`) · bottom (ChatInput · testid `chat-input`)
5. session 多轮跑通: sessionStorage(`cast_session`) + url query (?s=) 双持久化 · 同 sid 反复 POST /api/agent/run · 第 2 轮 LLM 真见到第 1 轮上下文 (暗号 turn 2 验)
6. e2e 砍重写: 删 `e2e/create-*.spec.ts` · 加 `e2e/chat-multi-turn.spec.ts` (≥ 1 case 真过 · 验 header 渲染 + sid 持久化 + 多轮真续 + 重开 + 暗号断言)
7. README + CONTRACTS.md 同 PR 改 (路由表只剩 `/` · CONTRACTS 砍 cast-api 业务 endpoint 段 · 加"间接依赖 · 通过 meta tool"段)

## 工程要求

- branch=feat/single-page-chat · base origin/main · 不是 origin/develop (origin/develop 还有 REQ-002 残留)
- TS build pass (`npm run build`)
- e2e `chat-multi-turn.spec.ts` 真过 (单 chromium · 60s 内)
- staging deploy 真跑过 (push develop · 看 staging.m.cast.agentaily.com)
- close PR #2 (REQ-002 那版 superseded)
- 不动 cast-api / cast-agents / meta yaml

## 完工 4 问

待完工后填。
