# REQ-002 · 砍 /create 页 + 阿空小造在聊天列表置顶

- 状态: in-progress
- assignee: cast-app-maintainer
- branch: feat/pin-meta-in-conversations
- base: origin/main
- parent_scenario: (lead 派单 5-9 · 老板 5-9 拍 UX 简化 · 砍 ➕ 入口 + 在聊天列表顶部置顶系统会话"阿空小造")

## 描述

老板拍 UX 简化:
- 砍 ➕ 号入口 / 砍 /create 路由 / 砍 CreateRolePage
- 在聊天列表顶部**置顶**一个不可删除的系统会话"阿空小造" (ag_builtin_meta-xiaozao)
- 用户点进去 = 普通的单聊天页 (新增 ConversationDetailPage) · 跟它聊就能造 agent
- UX 同 ChatGPT 主页: 顶部钉系统助手 · 不需要"创建"按钮

后端不变 (cast-api / cast-agents 不动) · 只改 cast-app UI 重组。

## 上游 ready

- cast-agents `POST /api/agent/run` (sync · 多轮 session 持久化) — 不变
- ag_builtin_meta-xiaozao 是 cast-api 真存在的 builtin agent — 不变

## acceptance (lead 锁 · ≤ 7 条)

1. /create 路由 + CreateRolePage.tsx 砍干净 · 各页 (HomePage / MePage / AppShell BottomNav) "造一个 / 去造艺人 / ➕" 入口全删
2. MessagesPage 列表顶部出现一条置顶项 "阿空小造" · data-testid=`pinned-meta-xiaozao` · 含 pin 标识 + 副标题 "帮你打造你的 AI 角色" · 不可删除 (无删除按钮 / 无长按菜单)
3. 点击置顶项 → 路由 /messages/ag_builtin_meta-xiaozao · 进 ConversationDetailPage · 加载多轮对话 UI
4. ConversationDetailPage: agent_id 从 url params 取 · session_id = `s_${agent_id}_${user_id_safe}` (跨刷新 / 跨页持久化 · sessionStorage 存) · 同前 CreateRolePage 多轮逻辑 (POST /api/agent/run · 只渲 final_text · 重开按钮清 session)
5. 输入消息 → 真打 /api/agent/run · ≥ 2 轮真续上下文 (暗号 turn 2 验) · LLM 真返非空 final_text
6. 删 e2e/create-*.spec.ts · 加 e2e/conversations-meta-xiaozao.spec.ts (≥ 1 case): 进 /messages → 看到 pinned-meta-xiaozao → 点进 ConversationDetailPage → 输入 → LLM 真返 → 重开后 session_id 换
7. README + CONTRACTS.md 同 PR 改 · 路由表删 /create · 加 /messages/:agentId · CONTRACTS 删"/create 多轮"段 · 加"meta-xiaozao 在聊天列表置顶"段

## 工程要求

- branch=feat/pin-meta-in-conversations · base origin/main · push develop 验 staging
- TS build pass (npm run build)
- e2e 至少 conversations-meta-xiaozao.spec.ts 真过
- 不动 cast-api / cast-agents

## 完工 4 问

待完工后填。
